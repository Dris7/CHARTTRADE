# Optional. Without a key the FRED CSV gateway (`fredgraph.csv`) is used and
# covers everything the dashboard needs. With a key, the JSON API is used,
# which has higher rate limits.
FRED_API_KEY=""
